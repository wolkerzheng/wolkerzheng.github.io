# Wolker Lab

一个部署在 GitHub Pages 上的中文科技风博客站，当前聚焦 `AI`、`智能体` 和 `Harness Engineering`。

更完整的站点维护说明见 [README.txt](./README.txt)。现在文章支持通过 `content/posts/*.md` 加 frontmatter 后，再执行 `node scripts/build-posts.mjs` 自动生成页面。
