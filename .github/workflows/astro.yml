name: Disabled legacy Astro Pages workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This legacy workflow is intentionally disabled. Use .github/workflows/deploy.yml instead."
